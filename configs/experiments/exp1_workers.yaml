trainer:
  lr: 0.001
  batch_size: 32
  epochs: 1
  model_path: models/model.pth
  num_workers: 2

model:
  name: baseline

data:
  path: data/

wandb:
  entity: Dynamic_Duo
  project: Pneumonia-Classification

loguru:
  log_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  level: INFO