trainer:
  lr: 0.00001
  batch_size: 32
  epochs: 5
  model_path: models/model.pth
  num_workers: 0

model:
  name: baseline

data:
  path: data/

wandb:
  entity: Dynamic_Duo
  project: Pneumonia-Classification

loguru:
  log_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  level: INFO